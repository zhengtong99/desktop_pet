# Runtime dependencies for the Desktop Pet app.
# End users who download a pre-built app do NOT need any of these.
# These are only needed to run from source.
PySide6>=6.6
requests>=2.28
lunardate>=0.2.2
